dependencies {

    api project(':misty-utils')

    implementation 'org.springframework.boot:spring-boot-starter-data-redis'

    testImplementation group: 'it.ozimov', name: 'embedded-redis', version: '0.7.3'

}
